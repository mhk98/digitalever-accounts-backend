const TeamFilterAbleFields = [
  "searchTerm",
  "name",
  "code",
  "status",
  "departmentId",
];

const TeamSearchableFields = ["name", "code", "description"];

module.exports = {
  TeamFilterAbleFields,
  TeamSearchableFields,
};
